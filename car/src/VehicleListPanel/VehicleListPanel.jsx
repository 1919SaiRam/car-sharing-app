import React from "react";
import { useState, useCallback } from "react";
import VDetails from '/VDetails';


var = Vehicleitems({ carOwner Id, Confirmation } : {

  const handleSubmit = useCallback((orderDetails) => {
   `{'/id/' + carOwner Id + '/book', }` 

      orderDetails,

    });

  }, [carOwner Id, Confirmation ] ) ;


 const VDetails () => async (
     userdata =  fetchData.then(VehicleDetails);
     console.log("VehicleDetails Noted")
 )


function VehicleList() {

    const [State, setState] = useState(null);
    return ( 
     
        <div className="Vehiclelist">
         
         <span className="VehNote"> {
            App.map(VehicleDetails)}
         <VDetails />
         </span> 

           <submit> onClickSubmit = { } </submit>
        </div>

    );
}

export default VehicleList ;

